name: App am Leben erhalten

on:
  schedule:
    # Alle 4 Stunden, um absolut sicherzugehen
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  ping_app:
    runs-on: ubuntu-latest
    steps:
      - name: Sende Weckruf
        run: |
          curl -L -v -m 30 https://lennartvoss.streamlit.app/
